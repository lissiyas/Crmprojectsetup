
  # CRM Project Setup

  This is a code bundle for CRM Project Setup. The original project is available at https://www.figma.com/design/i7KIWLU3PBcNSTlVMNtVg0/CRM-Project-Setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  